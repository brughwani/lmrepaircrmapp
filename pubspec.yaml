name: lmrepaircrmapp
description: "Service centre CRM"

version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  intl:
  js:
  flutter:
    sdk: flutter
  http:
  geolocator: ^13.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
